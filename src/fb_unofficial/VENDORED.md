Vendored from py-fb-unofficial (https://github.com/Walkercito/py-fb-unofficial).
This is a private library that cannot be installed from a public index, so it
is copied in-tree. It is EXCLUDED from this project's ruff/ty/pylint gate
(see pyproject.toml). Re-sync by re-copying src/fb_unofficial from that repo.
